# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.9

# Config
python-dotenv>=1.0.0
pydantic>=2.0.0

# PDF parsing
PyMuPDF>=1.23.0

# OpenAI (embeddings + GPT-4o)
openai>=1.0.0

# NLP / text chunking
nltk>=3.8.0

# LangChain + Qdrant
langchain-openai>=0.1.0
langchain-qdrant>=0.1.0
qdrant-client>=1.7.0

# ML / graph layout
scikit-learn>=1.3.0
numpy>=1.24.0

# PyTorch — install the GPU build from https://pytorch.org/get-started/locally/
# if you need CUDA support. The line below installs the CPU/MPS build from PyPI.
torch>=2.1.0

# Hugging Face Transformers
transformers>=4.40.0
accelerate>=0.27.0
